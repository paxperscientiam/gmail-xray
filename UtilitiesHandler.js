function formatDateService(date) {
  return Utilities.formatDate(date,'GMT',"E, d MMM y"); 
}

function formatTimeService(date) {
  return Utilities.formatDate(date, 'GMT', "h:mm a");
}

function formatAge(startDate) {
 var currentTime = new Date();
  
}
